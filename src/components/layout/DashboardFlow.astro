<main class="flow-shell">
  <section class="flow-grid" aria-label="navfolio personal dashboard">
    <aside class="flow-column side-column">
      <slot name="profile" />
      <slot name="links" />
    </aside>

    <section class="flow-column main-column">
      <slot name="hero" />
      <slot name="recently" />
    </section>

    <aside class="flow-column side-column">
      <slot name="clock" />
      <slot name="activity" />
    </aside>
  </section>
</main>

<style>
  .flow-shell {
    width: min(100%, 1500px);
    margin: 0 auto;
    padding: 76px 24px 46px;
  }

  .flow-grid {
    display: grid;
    grid-template-columns: minmax(250px, 0.9fr) minmax(460px, 2fr) minmax(280px, 1fr);
    gap: 18px;
    align-items: start;
  }

  .flow-column {
    display: grid;
    gap: 18px;
    align-content: start;
    min-width: 0;
  }

  .flow-grid :global(.glass-card) {
    width: 100%;
  }

  .flow-grid :global(.profile-card) {
    min-height: 302px;
    padding: 28px 22px 26px;
  }

  .flow-grid :global(.avatar-wrap) {
    width: 136px;
    height: 136px;
    margin-bottom: 18px;
  }

  .flow-grid :global(.profile-heading h1) {
    font-size: 1.78rem;
  }

  .flow-grid :global(.intro-card) {
    min-height: 350px;
    padding: 42px 44px;
  }

  .flow-grid :global(.clock-card) {
    min-height: 162px;
    padding: 24px 26px;
  }

  @media (max-width: 1180px) {
    .flow-grid {
      grid-template-columns: minmax(260px, 0.9fr) minmax(420px, 1.6fr);
    }

    .flow-grid > .flow-column:last-child {
      grid-column: 1 / -1;
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }
  }

  @media (max-width: 820px) {
    .flow-shell {
      padding: 58px 12px 24px;
    }

    .flow-grid,
    .flow-grid > .flow-column:last-child {
      grid-template-columns: 1fr;
    }

    .flow-grid :global(.intro-card) {
      min-height: auto;
      padding: 32px 26px;
    }
  }
</style>
