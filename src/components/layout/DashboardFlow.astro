<main class="w-full max-w-[1168px] mx-auto pt-[132px] pb-[72px] px-[32px]" data-pagefind-body>
  <section class="grid grid-cols-[minmax(0,760px)_minmax(240px,286px)] justify-center gap-10.5 items-start" aria-label="navfolio personal publication">
    <section class="grid content-start min-w-0 main-column">
      <slot name="hero" />
      <slot name="recently" />
    </section>

    <aside class="grid content-start min-w-0 side-column">
      <slot name="profile" />
      <slot name="activity" />
      <slot name="links" />
    </aside>
  </section>
</main>
