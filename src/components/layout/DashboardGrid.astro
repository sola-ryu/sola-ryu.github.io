<main class="dashboard-shell">
  <section class="orbit-stage" aria-label="navfolio personal orbit">
    <div class="orbit-ring orbit-ring--outer" aria-hidden="true"></div>
    <div class="orbit-ring orbit-ring--inner" aria-hidden="true"></div>
    <span class="orbit-dot orbit-dot--one" aria-hidden="true"></span>
    <span class="orbit-dot orbit-dot--two" aria-hidden="true"></span>
    <span class="orbit-dot orbit-dot--three" aria-hidden="true"></span>
    <span class="orbit-star orbit-star--one" aria-hidden="true">*</span>
    <span class="orbit-star orbit-star--two" aria-hidden="true">*</span>

    <aside class="orbit-slot orbit-slot--profile">
      <slot name="profile" />
    </aside>
    <aside class="orbit-slot orbit-slot--clock">
      <slot name="clock" />
    </aside>
    <section class="orbit-slot orbit-slot--hero">
      <slot name="hero" />
    </section>
    <aside class="orbit-slot orbit-slot--activity">
      <slot name="activity" />
    </aside>
    <aside class="orbit-slot orbit-slot--links">
      <slot name="links" />
    </aside>
    <aside class="orbit-slot orbit-slot--recently">
      <slot name="recently" />
    </aside>
  </section>
</main>

<style>
  .dashboard-shell {
    width: min(100%, 1540px);
    margin: 0 auto;
    padding: 52px 18px 32px;
  }

  .orbit-stage {
    position: relative;
    min-height: 900px;
  }

  .orbit-ring {
    position: absolute;
    left: 50%;
    top: 47%;
    pointer-events: none;
    border: 1px solid rgba(70, 183, 176, 0.24);
    border-radius: 50%;
    transform: translate(-50%, -50%) rotate(-7deg);
  }

  .orbit-ring--outer {
    width: min(94vw, 1410px);
    height: 520px;
    opacity: 0.76;
    animation: orbit-breathe 11s ease-in-out infinite;
  }

  .orbit-ring--inner {
    width: min(68vw, 940px);
    height: 350px;
    border-style: dashed;
    opacity: 0.34;
    transform: translate(-50%, -46%) rotate(13deg);
    animation: orbit-breathe 13s ease-in-out infinite reverse;
  }

  .orbit-dot,
  .orbit-star {
    position: absolute;
    z-index: 1;
    pointer-events: none;
  }

  .orbit-dot {
    width: 24px;
    height: 24px;
    border-radius: 999px;
    background: radial-gradient(circle at 35% 28%, #ffffff 0 12%, #61d7ad 46%, #26b98b 100%);
    box-shadow: 0 12px 28px rgba(32, 176, 142, 0.3);
    animation: float-soft 6s ease-in-out infinite;
  }

  .orbit-dot--one {
    left: 26%;
    top: 18%;
  }

  .orbit-dot--two {
    right: 7%;
    top: 11%;
    width: 34px;
    height: 34px;
  }

  .orbit-dot--three {
    left: 17%;
    bottom: 9%;
    width: 36px;
    height: 36px;
    animation-delay: -2s;
  }

  .orbit-star {
    color: #a982ff;
    font-size: 2.2rem;
    line-height: 1;
    text-shadow: 0 10px 22px rgba(169, 130, 255, 0.28);
    animation: float-soft 7s ease-in-out infinite;
  }

  .orbit-star--one {
    left: 21%;
    top: 14%;
  }

  .orbit-star--two {
    right: 15%;
    bottom: 6%;
    animation-delay: -3s;
  }

  .orbit-slot {
    position: absolute;
    z-index: 2;
    width: var(--slot-width);
    animation: float-soft var(--float-duration, 7s) ease-in-out infinite;
    animation-delay: var(--float-delay, 0s);
  }

  .orbit-slot:hover,
  .orbit-slot:focus-within {
    z-index: 12;
  }

  .orbit-slot--profile {
    --slot-width: 272px;
    --float-duration: 8s;
    left: 9.5%;
    top: 42px;
    transform: rotate(-5deg);
  }

  .orbit-slot--clock {
    --slot-width: 260px;
    --float-duration: 7.5s;
    --float-delay: -1.5s;
    right: 10.2%;
    top: 0;
    transform: rotate(7deg);
  }

  .orbit-slot--hero {
    --slot-width: min(42vw, 640px);
    z-index: 5;
    left: 50%;
    top: 118px;
    transform: translateX(-50%) rotate(-1.5deg);
  }

  .orbit-slot--activity {
    --slot-width: 382px;
    --float-duration: 8.5s;
    --float-delay: -2.5s;
    right: 2.2%;
    top: 224px;
    transform: rotate(-3.5deg);
  }

  .orbit-slot--links {
    --slot-width: 292px;
    --float-duration: 9s;
    --float-delay: -1s;
    left: 8%;
    bottom: 138px;
    transform: rotate(4deg);
  }

  .orbit-slot--recently {
    --slot-width: 340px;
    --float-duration: 8s;
    --float-delay: -4s;
    right: 3.2%;
    bottom: 8px;
    transform: rotate(2.5deg);
  }

  .orbit-slot :global(.glass-card) {
    width: 100%;
    background: linear-gradient(145deg, rgba(255, 255, 255, 0.7), rgba(255, 255, 255, 0.36));
    border-color: rgba(255, 255, 255, 0.74);
  }

  .orbit-slot--hero :global(.intro-card) {
    min-height: 350px;
    padding: 42px 44px;
    box-shadow:
      0 34px 90px rgba(83, 132, 157, 0.2),
      0 0 80px rgba(202, 246, 239, 0.46),
      inset 0 1px 1px rgba(255, 255, 255, 0.76);
  }

  .orbit-slot--profile :global(.profile-card) {
    min-height: 302px;
    padding: 28px 22px 26px;
  }

  .orbit-slot--profile :global(.avatar-wrap) {
    width: 136px;
    height: 136px;
    margin-bottom: 18px;
  }

  .orbit-slot--profile :global(.profile-heading h1) {
    font-size: 1.78rem;
  }

  .orbit-slot--clock :global(.clock-card) {
    min-height: 162px;
    padding: 24px 26px;
  }

  .orbit-slot--activity :global(.heatmap-card),
  .orbit-slot--recently :global(.doing-card),
  .orbit-slot--links :global(.nav-card) {
    background: linear-gradient(145deg, rgba(255, 255, 255, 0.66), rgba(255, 255, 255, 0.3));
  }

  @keyframes float-soft {
    0%,
    100% {
      margin-top: 0;
    }

    50% {
      margin-top: -8px;
    }
  }

  @keyframes orbit-breathe {
    0%,
    100% {
      opacity: 0.32;
    }

    50% {
      opacity: 0.72;
    }
  }

  @media (max-width: 1240px) {
    .orbit-stage {
      min-height: 1120px;
    }

    .orbit-slot--profile {
      left: 4%;
    }

    .orbit-slot--hero {
      --slot-width: min(56vw, 610px);
    }

    .orbit-slot--clock {
      right: 3%;
    }

    .orbit-slot--activity {
      right: 1%;
      top: 560px;
    }

    .orbit-slot--links {
      left: 5%;
      bottom: 190px;
    }

    .orbit-slot--recently {
      right: 0;
      bottom: 84px;
    }
  }

  @media (max-width: 900px) {
    .dashboard-shell {
      padding: 58px 12px 24px;
    }

    .orbit-stage {
      display: grid;
      gap: 18px;
      min-height: 0;
    }

    .orbit-ring,
    .orbit-dot,
    .orbit-star {
      display: none;
    }

    .orbit-slot {
      position: static;
      width: 100%;
      transform: none;
      animation: none;
    }

    .orbit-slot--hero {
      order: -2;
    }

    .orbit-slot--clock {
      order: -1;
    }

    .orbit-slot--hero :global(.intro-card) {
      min-height: auto;
      padding: 32px 26px;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .orbit-slot,
    .orbit-ring,
    .orbit-dot,
    .orbit-star {
      animation: none;
    }
  }
</style>
